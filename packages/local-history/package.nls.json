{
  "description": "Records local file history and restores files or folders to a past state — like WebStorm's Local History",
  "command.showHistory.title": "Show History",
  "command.rollback.title": "Rollback to This State",
  "view.name": "Local History",
  "config.exclude.description": "Glob patterns to exclude from local history, in addition to files.exclude and files.watcherExclude. On startup, entries matching the current patterns are also removed from existing history."
}
