{
  "name": "simplysm-tasks",
  "displayName": "Simplysm Tasks",
  "version": "0.0.29",
  "private": true,
  "license": "Apache-2.0",
  "description": "%description%",
  "publisher": "simplysm",
  "repository": {
    "type": "git",
    "url": "https://github.com/simplysm/simplysm-vscode.git",
    "directory": "packages/tasks"
  },
  "files": [
    "dist/**/*.cjs",
    "dist/webview/**/*.js",
    "dist/webview/**/*.css",
    "dist/webview/**/*.ttf",
    "images/icon.png",
    "images/*.gif",
    "l10n/*.json",
    "package.nls.json",
    "package.nls.ko.json"
  ],
  "type": "module",
  "main": "./dist/extension.cjs",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test \"src/**/*.test.ts\""
  },
  "devDependencies": {
    "@vscode/codicons": "^0.0.45"
  },
  "l10n": "./l10n",
  "contributes": {
    "languages": [
      {
        "id": "simplysm-tasks",
        "aliases": [
          "Simplysm Tasks"
        ],
        "extensions": [
          ".tasks"
        ]
      }
    ],
    "customEditors": [
      {
        "viewType": "simplysm-tasks.editor",
        "displayName": "%editor.displayName%",
        "selector": [
          {
            "filenamePattern": "*.tasks"
          }
        ],
        "priority": "default"
      }
    ],
    "commands": [
      {
        "command": "simplysm-tasks.new",
        "title": "%command.new.title%",
        "category": "Tasks"
      }
    ],
    "keybindings": [
      {
        "key": "ctrl+z",
        "mac": "cmd+z",
        "command": "simplysm-tasks.historyKey",
        "args": { "action": "undo" },
        "when": "activeCustomEditorId == 'simplysm-tasks.editor' && !inputFocus"
      },
      {
        "key": "ctrl+shift+z",
        "mac": "cmd+shift+z",
        "command": "simplysm-tasks.historyKey",
        "args": { "action": "redo" },
        "when": "activeCustomEditorId == 'simplysm-tasks.editor' && !inputFocus"
      },
      {
        "key": "ctrl+y",
        "command": "simplysm-tasks.historyKey",
        "args": { "action": "redo" },
        "when": "activeCustomEditorId == 'simplysm-tasks.editor' && !inputFocus"
      }
    ],
    "menus": {
      "file/newFile": [
        {
          "command": "simplysm-tasks.new"
        }
      ]
    }
  },
  "icon": "images/icon.png",
  "engines": {
    "vscode": "^1.127.0"
  }
}
