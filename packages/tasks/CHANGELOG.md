# Changelog

## 1.0.2 - 2026-08-10

- chore: deploy:local keeps package.json versions untouched (vsix-only bump)
- Drag a tab within the tab bar to reorder it, with an insert line instead of a split preview

## 1.0.0 - 2026-08-07

Initial release.

- Quick list editor for `.tasks` memo files
- Undo/redo support (Ctrl+Z / Ctrl+Shift+Z / Ctrl+Y)
- New file entry in the File > New File menu
